spring:
  application:
    name: facture-service
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:Facture-service
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
  cloud:
    discovery:
      enabled: true

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: "*"
